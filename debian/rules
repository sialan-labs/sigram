#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_configure:
	qmake -- PREFIX=/usr

override_dh_install:
	dh_install

override_dh_clean:
	dh_clean
